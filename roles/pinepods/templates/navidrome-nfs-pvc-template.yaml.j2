apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ navidrome_pvc_name }}
  namespace: navidrome
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-storage

